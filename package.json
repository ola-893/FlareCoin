{
  "name": "flareyield-manager",
  "version": "1.0.0",
  "private": true,
  "description": "FlareYield Manager — Root workspace runner for Frontend and Off-chain Executor Service",
  "workspaces": [
    "frontend",
    "executor",
    "fce-extension"
  ],
  "scripts": {
    "postinstall": "npm run install:all",
    "install:all": "npm install --prefix frontend && npm install --prefix executor && npm install --prefix fce-extension && npm run build --prefix fce-extension",
    "start:all": "./start-all.sh",
    "stop:all": "./start-all.sh --stop",
    "docker:up": "bash ./start-docker.sh",
    "start": "npx -y concurrently -n \"DOCKER,FRONTEND,EXECUTOR,FCE-EXT,NGROK\" -c \"blue,cyan,magenta,green,yellow\" \"npm run docker:up\" \"npm run dev --prefix frontend\" \"npm start --prefix executor\" \"npm start --prefix fce-extension\" \"ngrok http 8080 --log=stdout\"",
    "dev": "npm start",
    "frontend": "npm run dev --prefix frontend",
    "executor": "npm start --prefix executor",
    "fce-extension": "npm start --prefix fce-extension",
    "rebalance:retry": "node script/trigger-rebalance.mjs",
    "rebalance:relay": "node script/relay-rebalance.mjs",
    "build": "npm run build --prefix frontend",
    "preflight:release": "node script/preflight-release.mjs",
    "upgrade:fxrp-vault": "node script/upgrade-fxrp-vault-fee.mjs"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "viem": "^2.55.11"
  }
}
